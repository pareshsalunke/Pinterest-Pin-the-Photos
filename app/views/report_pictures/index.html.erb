<h1>Listing report_pictures</h1>

<table>
  <tr>
    <th>Rep description</th>
    <th>Action taken</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @report_pictures.each do |report_picture| %>
  <tr>
    <td><%= report_picture.rep_description %></td>
    <td><%= report_picture.action_taken %></td>
    <td><%= link_to 'Show', report_picture %></td>
    <td><%= link_to 'Edit', edit_report_picture_path(report_picture) %></td>
    <td><%= link_to 'Destroy', report_picture, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Report picture', new_report_picture_path %>
